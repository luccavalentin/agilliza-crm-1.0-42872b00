<!-- Agilliza — instruções internas para agentes de código -->

> Ao trabalhar neste repositório, siga as convenções documentadas em `MDs/` e
> em `mem://index.md`. Não reescreva o histórico do branch publicado (evite
> force-push, rebase/amend/squash em commits já enviados) para não corromper
> o histórico do projeto.
